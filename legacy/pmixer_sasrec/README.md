# Legacy SASRec implementation

These files are the original baseline that was present at the repository root. The implementation follows the PyTorch SASRec code published by the `pmixer/SASRec.pytorch` project.

They are retained only for provenance and comparison. The `ftrec` package does not import or execute this directory.

